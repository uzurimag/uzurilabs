# supabase/migrations

SQL migration files for the Supabase database, managed via the Supabase CLI (`supabase migration new <name>`). No migrations exist yet — schema and auth are out of scope for ENG-000.
